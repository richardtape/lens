// ContentView.swift — Thin wrapper that returns the real navigation shell.
// Phase 1 placeholder replaced in Phase 3.
import SwiftUI
import LensUI

struct ContentView: View {
    var body: some View {
        RootTabView()
    }
}
